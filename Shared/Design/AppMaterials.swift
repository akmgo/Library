#if os(macOS) || os(iOS)
import SwiftUI

enum AppMaterials {
    static let background = Material.ultraThin
    static let card = Material.thin
    static let detail = Material.regular
    static let modal = Material.regular
    static let control = Material.ultraThin
}
#endif
